# Open threads

Stable ids; never renumber. Closed items move to the bottom with the date.

| id  | thread                                                                                                                                                                  | why deferred                                          | resumes when                 |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ---------------------------- |
| T1  | Manual smoke test of v2.0.0 in Chrome (load `dist/` unpacked): loop, tail, both speed modes, Space, fragments, saved drawer, chart, panel drag/persist, SPA navigation. | Needs a human in the browser.                         | The user reports the result. |
| T2  | Tests for `player/` and `ui/` modules beyond pure helpers.                                                                                                              | Behaviour of the DOM layer is only confirmable by T1. | T1 passes.                   |
| T3  | `LICENSE` file.                                                                                                                                                         | Choice of licence is the author's.                    | The user picks one.          |
| T4  | Move `example.html` under `docs/fixtures/` or drop it.                                                                                                                  | It is referenced only by humans; see `docs/KEEP.md`.  | The user decides.            |
